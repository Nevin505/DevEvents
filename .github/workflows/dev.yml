name: Dev Events
on: [push]
jobs:
  sample_execution_jobs:
    runs-on: ["self-hosted", "linux", "x64"]
    steps:
      - uses: actions/checkout@v4
      - name: Use Node js
        uses: actions/setup-node@v4
        with:
          node-version: "20.x"
      - run: echo "hi testing this pipeline"
      - run: npm i
      - run: pm2 start "npm start"
